# Ollama MCP Server Configuration

# Ollama API endpoint
# For Docker on Windows/Mac: http://host.docker.internal:11434 (recommended)
# For local Ollama: http://localhost:11434
# For Docker on Linux: http://172.17.0.1:11434 (or your bridge IP)
OLLAMA_API=http://host.docker.internal:11434

# Silence startup messages (for MCP compatibility)
# Set to 'true' to prevent startup logs that can interfere with MCP protocol
SILENCE_STARTUP=true

# Optional: Enable debug logging (set to 'true' for verbose logs)
DEBUG=false
